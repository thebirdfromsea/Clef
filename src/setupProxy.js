/*const proxy = require('http-proxy-middleware');

module.exports = function (app) {
    app.use(proxy('/Home', {
        target: 'localhost:8888/login',
        headers: {
            "Connection": "keep-alive"
        },
    }));
};*/